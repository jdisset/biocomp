from biocomp import utils as ut
import scriptutils as su
import biocomp.datautils as du

# 1. load the library pickle (look at load_lib function). 
#    There are some default paths you can declare and use so that 
#    you don't have to specify the full path. 

# 2. try to load one of the recipe files into a network directly, 
#    then try to examine its .compute_graph object! (look at the recipe.py anbd network.py files)


